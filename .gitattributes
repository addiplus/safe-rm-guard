# Normalize line endings to LF in the repo (silences the CRLF warnings on Windows
# checkouts) while letting Git handle the working-copy conversion.
* text=auto eol=lf
*.py text eol=lf
*.md text eol=lf
